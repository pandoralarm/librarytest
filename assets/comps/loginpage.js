const { createApp } = Vue

  createApp({
    data() {
      return {
        message: 'Hello Vue!',
        target: 'login',
        registeralert: '',
      }
    }
    })
    .methods: {

    }
    mount('#loginpage')

